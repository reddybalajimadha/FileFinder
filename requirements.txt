# === Core search (no extra deps - SQLite is built into Python) ===

# === Desktop application ===
pywebview>=4.0           # Native window wrapper
fastapi>=0.100.0          # Web API framework
uvicorn>=0.23.0           # ASGI server
pydantic>=2.0             # Request/response models

# === Document text extraction ===
pdfplumber>=0.9.0         # Native PDF text (fast, preferred)
python-docx>=0.8.11       # DOCX support

# === Optional: OCR for scanned PDFs ===
# Also requires system packages: tesseract-ocr, poppler-utils
pytesseract>=0.3.10
pdf2image>=1.16.0

# === Optional: AI semantic search ===
# Large downloads (~500MB+). Only needed for --semantic flag.
sentence-transformers>=2.2.0
torch>=1.9.0

# === Optional: Building Windows .exe ===
pyinstaller>=5.0          # For creating standalone .exe
